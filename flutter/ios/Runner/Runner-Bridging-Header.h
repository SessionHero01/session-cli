#import "GeneratedPluginRegistrant.h"

#include <stdint.h>

uint16_t global_service_shared_get_port(
                                        const unsigned char* psk_data,
                                        size_t psk_data_len,
                                        const unsigned char*data_dir,
                                        size_t data_dir_len,
                                        char * error_buf,
                                        size_t error_buf_len
                                        );
